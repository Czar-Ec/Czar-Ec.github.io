import { InjectionToken } from '@angular/core';

// external links config
export const EXTERNAL_URLS = new InjectionToken<any>('EXTERNAL_URLS');

// project preview config
export const PROJECT_PREVIEW = new InjectionToken<any>('PROJECT_PREVIEW');

// icons config
export const ICON_CONFIG = new InjectionToken<any>('ICON_CONFIG');

// tools config
export const TOOLS_CONFIG = new InjectionToken<any>('TOOLS_CONFIG');
